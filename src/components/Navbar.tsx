const Navbar = () => (
    <nav className="pointer-events-auto w-full flex justify-between items-center py-4 px-8 z-10">
        <span className="text-white text-[2rem] font-medium cursor-pointer">Fusion</span>
        <button className="bg-black text-white w-[6.5vw] h-[5vh] rounded-full text-[1.2rem]
        font-light cursor-pointer">
            Let's talk
        </button>
    </nav>
);

export default Navbar;
